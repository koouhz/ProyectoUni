<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= typeof title !== 'undefined' ? title : 'Proyecto' %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <header>
    <nav class="container">
      <ul>
        <li><a href="/">Inicio</a></li>
        <li><a href="/usuarios">Usuarios</a></li>
        <li><a href="/servicios">Servicios</a></li>
        <li><a href="/contacto">Contacto</a></li>
      </ul>
    </nav>
  </header>

  <main class="container">
    <% if (typeof usuarios !== 'undefined' && usuarios.length > 0) { %>
      <h1>Lista de Usuarios</h1>
      <ul>
        <% usuarios.forEach(function(usuario) { %>
          <li><%= usuario.Nombre %> <%= usuario.Apellido1 %> (<%= usuario.Correo %>)</li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>No hay usuarios disponibles.</p>
    <% } %>
  </main>

  <footer>
    <div class="container">
      &copy; <%= new Date().getFullYear() %> Proyecto - Todos los derechos reservados.
    </div>
  </footer>

  <script src="/js/script.js"></script>
</body>
</html>